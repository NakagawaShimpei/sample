# WEソリューション 機能・画面遷移まとめ

## 画面一覧

| パス | 画面名 | アクセス |
|------|--------|---------|
| `/login` | ログイン | 未認証のみ |
| `/rooms` | 会議室一覧 | 全員 |
| `/rooms/:id/reserve` | 会議室予約 | 全員 |
| `/reservations` | 予約一覧 | 全員 |
| `/rooms/register` | 会議室登録 | **管理者のみ** |
| `/devices` | デバイス一覧 | 全員 |
| `/devices/register` | デバイス登録 | **管理者のみ** |
| `/loans` | 貸出状況 | **管理者のみ** |
| `/users` | ユーザー一覧 | **管理者のみ** |
| `/users/register` | ユーザー登録 | **管理者のみ** |

---

## 利用者（user ロール）ができる操作

### 会議室
- 会議室一覧の閲覧（名前・場所・定員・設備・当日の空き状況）
- 会議室の予約（日時・参加人数・会議名・参加者を入力）
- **自分の予約のみ**キャンセル

### デバイス
- デバイス一覧の閲覧（名前・種別・管理番号・場所・状態）
- 空きデバイスの貸出（`利用可能` → `使用中`）
- **自分が借りたデバイスのみ**返却

---

## 管理者（admin ロール）ができる追加操作

### 会議室
- 会議室の新規登録
- 会議室の削除（紐づく予約も一括削除）

### 予約
- **全員分の予約**を閲覧・キャンセル可能（利用者は自分のみ）

### デバイス
- デバイスの新規登録
- デバイスの削除（紐づく貸出も一括削除）
- **他ユーザーが借りているデバイスの強制返却**

### 貸出状況
- 現在の全貸出一覧を閲覧（読み取り専用）

### ユーザー管理
- ユーザー一覧の閲覧
- 新規ユーザー登録（role は常に `user` で作成）
- `user` ロールのアカウントのみ削除可（自分自身・他管理者は削除不可）

---

## 画面遷移

```
ログイン (/login)
  └─[認証成功]→ 会議室一覧 (/rooms)  ※ログイン後のデフォルト画面

会議室一覧 (/rooms)
  ├─[予約する]→ 会議室予約 (/rooms/:id/reserve)
  │                ├─[予約送信]→ 予約一覧 (/reservations)
  │                └─[キャンセル]→ 会議室一覧 (/rooms)
  └─[管理者][会議室登録]→ 会議室登録 (/rooms/register)
                              └─[登録送信]→ 会議室一覧 (/rooms)

予約一覧 (/reservations)
  └─[キャンセル]→ 同画面で更新（遷移なし）

デバイス一覧 (/devices)
  ├─[貸出/返却/強制返却]→ 同画面で更新（遷移なし）
  └─[管理者][デバイス登録]→ デバイス登録 (/devices/register)
                                └─[登録送信]→ デバイス一覧 (/devices)

ユーザー一覧 (/users)  ※管理者のみ
  └─[ユーザー登録]→ ユーザー登録 (/users/register)
                        └─[登録送信]→ ユーザー一覧 (/users)

貸出状況 (/loans)  ※管理者のみ・読み取り専用
```

---

## その他の共通仕様

- **削除・キャンセル・貸出・返却**はすべて `window.confirm` で確認ダイアログが出る
- **バリデーションエラー・API失敗**は `window.alert` で通知
- ページ上部のバナーでローディング状態・エラーを表示
- セッションは `sessionStorage` に保持（タブを閉じるとログアウト）
- テストアカウント: `user/user123`（利用者）、`admin/admin123`（管理者）

---

## バグ一覧

### セキュリティ（重大）

| # | ファイル | 内容 |
|---|---------|------|
| 1 | `src/contexts/AuthContext.tsx` | パスワードをサーバーからクライアントに丸ごと取得して平文比較している。DB・通信・ブラウザメモリすべてにパスワードが露出 |
| 2 | `src/pages/UserListPage.tsx` | ユーザー一覧画面にパスワードをそのまま表示している |
| 3 | `src/pages/UserRegisterPage.tsx` | パスワード入力欄が `type="text"` になっており、入力内容が画面に丸見え |

### ロジックバグ

| # | ファイル | 内容 |
|---|---------|------|
| 4 | `src/pages/RoomReservePage.tsx` | 予約フォームの初期値は `displayName` なのに、保存時は `username` を `reservedBy` に使っており不一致 |
| 5 | `src/pages/ReservationListPage.tsx` | 上記#4の影響で、`reservedBy === username` のフィルタが正しく機能せず、自分の予約が表示されない可能性がある |
| 6 | `src/contexts/DataContext.tsx` | `borrowDevice` でデバイスのステータスを `inUse` に更新した後、貸出レコード作成が失敗するとデバイスが `inUse` のまま宙に浮く（ロールバックなし） |
| 7 | `src/contexts/DataContext.tsx` | `deleteReservationsByRoom` / `deleteLoanByDevice` で `Promise.all` を使っており、途中の削除が失敗すると会議室・デバイスは消えたが関連データが残る不整合状態になる |

### バリデーション不足

| # | ファイル | 内容 |
|---|---------|------|
| 8 | `src/pages/RoomReservePage.tsx` | 終了時刻が開始時刻より前でも予約できる（例: 14:00〜13:00） |
| 9 | `src/pages/RoomReservePage.tsx` | 参加人数が会議室定員を超えていても予約できる |
| 10 | `src/pages/RoomReservePage.tsx` | 過去の日付でも予約できる |

### UI・操作バグ

| # | ファイル | 内容 |
|---|---------|------|
| 11 | `src/pages/RoomReservePage.tsx` | 「予約者名」フィールドをユーザーが手動で消してから送信するとバリデーションエラーになるが、本来このフィールドは編集不要（読み取り専用にすべき） |
| 12 | `src/App.tsx` | 存在しないURLにアクセスすると、ログイン済みでも `/login` にリダイレクトされる |

### その他

| # | ファイル | 内容 |
|---|---------|------|
| 13 | `src/api.ts` | `generateId` が `Date.now() + Math.random()` ベースのため、高速連続操作でIDが衝突する可能性がある |
| 14 | `src/api.ts` | 204レスポンスを `undefined as T` で返しており、データを返すべきエンドポイントで使うと壊れる |

### 優先度まとめ

- **今すぐ直すべき（セキュリティ）**: #1〜#3（パスワード関連）
- **機能に直結するバグ**: #4〜#7（データ不整合）、#8〜#10（バリデーション）
- **UX改善レベル**: #11〜#14
