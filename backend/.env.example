JWT_SECRET=replace-this-with-a-long-random-string-in-production
PORT=3002
NODE_ENV=development
INACTIVITY_TIMEOUT_MS=1800000
# TOTPシークレット暗号化キー: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" で生成
TOTP_ENCRYPTION_KEY=your-64-hex-char-key-here
