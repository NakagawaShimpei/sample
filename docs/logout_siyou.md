# ログアウト機能 仕様書

## 背景・目的

現状、一度ログインするとブラウザを閉じる以外にセッションを終了する手段がない。
社内の共用PCや貸出端末での利用を想定すると、前の利用者のアカウントが残ったまま別の人が操作できてしまうリスクがある。
ログアウト機能を追加することで、利用者が明示的にセッションを終了できるようにする。

---

## 方針

### セッション管理の方針

セッションは `sessionStorage` に保存されている。ログアウト時は `sessionStorage` からセッション情報を削除し、未認証状態に戻す。ブラウザタブを閉じると自動的にセッションが切れる `sessionStorage` の特性はそのまま維持する。

### ボタンの配置方針

ログアウトはナビゲーション（機能へのリンク群）ではなく、**ヘッダーのユーザー情報欄**に配置する。
ナビゲーションは「どのページに移動するか」を選ぶ場所であり、セッションの終了操作とは意味的に異なる。
「誰がログインしているか」が表示されている場所の近くに置くことで、操作の文脈が一致する。

### ログアウト後の画面遷移の方針

ログアウト後は `/login` にリダイレクトする。このとき、ブラウザの「戻る」ボタンでログアウト前の画面に戻れないよう、履歴を置き換える形で遷移する。セキュリティ上、ログアウト後に認証済み画面のキャッシュが表示されてしまうことを防ぐため。

---

## 影響範囲

| 対象 | 内容 |
|------|------|
| 認証コンテキスト | ログアウト処理の関数を追加する |
| ヘッダーコンポーネント | ログアウトボタンを追加し、クリック時にログアウト処理とリダイレクトを呼び出す |

---

## 検証観点

- ログイン後、ヘッダーにログアウトボタンが表示されること
- ボタンをクリックするとログイン画面に遷移すること
- ログアウト後にブラウザの「戻る」を押しても認証済み画面に戻れないこと
- ログアウト後、sessionStorage のセッション情報が削除されていること
- 一般ユーザー・管理者どちらでも同じ動作になること
